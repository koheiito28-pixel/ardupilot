# ardupilot's root .gitignore ignores index.html globally — re-include ours
# (it is the Vite entry point and must be committed for the build to work).
!index.html

node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# tsc -b build outputs
*.tsbuildinfo
vite.config.js
vite.config.d.ts
